import { Flex } from "@chakra-ui/react"
import type { NextPage } from "next"
import Head from "next/head"

export const Stake: NextPage = (props) => {
  return (
    <>
      <Head>
        <title>Celostrials | Stake</title>
      </Head>
      <Flex maxW="xl" px={6}></Flex>
    </>
  )
}

export default Stake
